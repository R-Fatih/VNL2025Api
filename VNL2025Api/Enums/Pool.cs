using System.ComponentModel.DataAnnotations;

namespace VNL2025Api.Enums
{
    public enum Pool
    {
        [Display(Name = "Pool 1")]
        Pool1,
        [Display(Name = "Pool 2")]
        Pool2,
        [Display(Name = "Pool 3")]
        Pool3,
        [Display(Name = "Pool 4")]
        Pool4,
        [Display(Name = "Pool 5")]
        Pool5,
        [Display(Name = "Pool 6")]
        Pool6,
        [Display(Name = "Pool 7")]
        Pool7,
        [Display(Name = "Pool 8")]
        Pool8,
        [Display(Name = "Pool 9")]
        Pool9,
    }
}
